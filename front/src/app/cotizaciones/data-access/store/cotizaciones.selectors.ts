import { cotizacionesFeature } from './cotizaciones.reducer';

export const {
  selectCreateStatus,
  selectCreateError,
  selectLastCreated,

  selectListStatus,
  selectListError,
  selectItems,
} = cotizacionesFeature;
